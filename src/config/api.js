/*
Config for paths to local assets and remote API url. Note that version,
 s in '/v1/somecall' should be appended in each component for flexibility
 */

const apiConfig = Object.freeze({
    apiUrl: 'https://api.silenceisgrand.com/api/',
    imagePath: '/images',
});

export default apiConfig;
