/*
Temporary file during development with some site configs
 */

const siteConfig = Object.freeze({
    siteOnline: false // defines however sites is in maintenance mode or not
});

export default siteConfig;
